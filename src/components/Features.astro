---
export interface Props {}

const features = [
  {
    title: "健康記録の一元管理",
    description: "予防接種、体重、食事内容など、愛犬の健康情報をすべてアプリに記録。必要な時にいつでも確認できます。",
    image: "./assets/feature-1.png"
  },
  {
    title: "症状の詳細記録",
    description: "便の状態、くしゃみ、下痢など、日々の体調変化を記録。獣医師との相談がスムーズになります。",
    image: "./assets/feature-2.png"
  },
  {
    title: "診察の準備が簡単",
    description: "病院への持参データをワンタップで表示。サンプル忘れ、情報伝え忘れの心配はもうありません。",
    image: "./assets/feature-3.png"
  }
];
---

<section class="features">
  <div class="container">
    <h2>3つの主な機能</h2>
    <div class="features-grid">
      {features.map((feature, index) => (
        <div class="feature-card" style={`animation-delay: ${index * 0.1}s`}>
          <div class="feature-image">
            <img src={feature.image} alt={feature.title} />
          </div>
          <h3>{feature.title}</h3>
          <p>{feature.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .feature-card {
    animation: fadeInUp 0.6s ease forwards;
  }
</style>
